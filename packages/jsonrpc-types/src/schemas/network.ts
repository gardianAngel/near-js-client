/**
 * Generated Zod schemas for NEAR Protocol JSON-RPC
 */

import { z } from 'zod';

export const RpcEXPERIMENTALgenesisconfigRequestSchema = z.record(z.unknown());

export const RpcEXPERIMENTALgenesisconfigResponseSchema = z.record(z.unknown());

export const RpcEXPERIMENTALprotocolconfigRequestSchema = z.record(z.unknown());

export const RpcEXPERIMENTALprotocolconfigResponseSchema = z.record(z.unknown());

export const RpcEXPERIMENTALvalidatorsorderedRequestSchema = z.record(z.unknown());

export const RpcEXPERIMENTALvalidatorsorderedResponseSchema = z.record(z.unknown());

export const RpcNetworkinfoRequestSchema = z.record(z.unknown());

export const RpcNetworkinfoResponseSchema = z.record(z.unknown());

export const RpcStatusRequestSchema = z.record(z.unknown());

export const RpcStatusResponseSchema = z.record(z.unknown());

export const RpcValidatorsRequestSchema = z.record(z.unknown());

export const RpcValidatorsResponseSchema = z.record(z.unknown());

