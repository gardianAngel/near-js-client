/**
 * Generated Zod schemas for NEAR Protocol JSON-RPC
 */

import { z } from 'zod';

export const RpcEXPERIMENTALchangesinblockRequestSchema = z.record(z.unknown());

export const RpcEXPERIMENTALchangesinblockResponseSchema = z.record(z.unknown());

export const RpcEXPERIMENTALlightclientblockproofRequestSchema = z.record(z.unknown());

export const RpcEXPERIMENTALlightclientblockproofResponseSchema = z.record(z.unknown());

export const RpcBlockRequestSchema = z.record(z.unknown());

export const RpcBlockResponseSchema = z.record(z.unknown());

export const RpcChunkRequestSchema = z.record(z.unknown());

export const RpcChunkResponseSchema = z.record(z.unknown());

export const RpcNextlightclientblockRequestSchema = z.record(z.unknown());

export const RpcNextlightclientblockResponseSchema = z.record(z.unknown());

