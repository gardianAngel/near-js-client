/**
 * Generated Zod schemas for NEAR Protocol JSON-RPC
 */

import { z } from 'zod';

export const RpcEXPERIMENTALchangesRequestSchema = z.record(z.unknown());

export const RpcEXPERIMENTALchangesResponseSchema = z.record(z.unknown());

export const RpcEXPERIMENTALcongestionlevelRequestSchema = z.record(z.unknown());

export const RpcEXPERIMENTALcongestionlevelResponseSchema = z.record(z.unknown());

export const RpcEXPERIMENTALlightclientproofRequestSchema = z.record(z.unknown());

export const RpcEXPERIMENTALlightclientproofResponseSchema = z.record(z.unknown());

export const RpcEXPERIMENTALmaintenancewindowsRequestSchema = z.record(z.unknown());

export const RpcEXPERIMENTALmaintenancewindowsResponseSchema = z.record(z.unknown());

export const RpcEXPERIMENTALsplitstorageinfoRequestSchema = z.record(z.unknown());

export const RpcEXPERIMENTALsplitstorageinfoResponseSchema = z.record(z.unknown());

export const RpcChangesRequestSchema = z.record(z.unknown());

export const RpcChangesResponseSchema = z.record(z.unknown());

export const RpcClientconfigRequestSchema = z.record(z.unknown());

export const RpcClientconfigResponseSchema = z.record(z.unknown());

export const RpcGaspriceRequestSchema = z.record(z.unknown());

export const RpcGaspriceResponseSchema = z.record(z.unknown());

export const RpcHealthRequestSchema = z.record(z.unknown());

export const RpcHealthResponseSchema = z.record(z.unknown());

export const RpcLightclientproofRequestSchema = z.record(z.unknown());

export const RpcLightclientproofResponseSchema = z.record(z.unknown());

export const RpcQueryRequestSchema = z.record(z.unknown());

export const RpcQueryResponseSchema = z.record(z.unknown());

